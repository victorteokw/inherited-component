const { classed } = require('./classed')
const { inherited } = require('./inherited')
const { withClasses } = require('./withClasses')
const { withProps } = require('./withProps')

module.exports = { classed, inherited, withClasses, withProps }
